services:
  vitara-web:
    image: nginx:alpine
    container_name: vitara-web
    restart: unless-stopped
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "5080:80"
